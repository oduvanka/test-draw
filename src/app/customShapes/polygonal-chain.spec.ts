import { PolygonalChainShape } from './polygonal-chain-shape';

describe('PolygonalChain', () => {
  it('should create an instance', () => {
    expect(new PolygonalChainShape()).toBeTruthy();
  });
});
